import Vue from 'vue'
import Vuex from 'vuex'
import initData from './modules/init'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    initData
  }
})
